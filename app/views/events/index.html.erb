<h1>Listing events</h1>

<table>
  <tr>
    <th></th>
    <th></th>
    <th></th>
  </tr>

<% @events.each do |event| %>
  <tr>
    <td><%= link_to 'Show', network_event_path(@network,event) %></td>
    <td><%= link_to 'Edit', edit_network_event_path(@network,event) %></td>
    <td><%= link_to 'Destroy', network_event_path(@network,event), method: :delete, data: { confirm: 'Are you sure?' } %></td>
  </tr>
<% end %>
</table>

<br />

<%= link_to 'New Event', new_network_event_path(@network) %>
